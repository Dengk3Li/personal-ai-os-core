# Changelog

版本变更记录集中放在这里。README 只保留长期稳定的产品说明、安装方式和边界。

## Unreleased

- 并发调度默认提供 5 个全局执行槽位，允许同一领域内资源互不冲突的任务并行推进。
- 每个任务在执行前一次性申请完整资源集合；路径重叠、活动任务资源未知或槽位不足时保持等待。
- 更新公开 Workbench 的字体层级：主标题与任务标题使用中文编辑字体，操作文字与系统标签分层显示。
- 保持米白纸面、森林绿品牌色和深海军蓝执行重点区的公开 LongTask 视觉语言。
- 工作详情补充基于持久化依赖关系的“前因 → 本轮结果 → 后果与下一步”阅读路径。

## 0.14.0

- 工作协议：在执行前校验指令、模板引用、执行规则和记忆主体边界。
- 后台设置：模型、路由、执行适配器和 API 配置集中在设置入口。
- 模块地图：区分系统全景与组件依赖，并保留上下层输入、输出和反馈连接。

## 0.13.0

- 工作方式候选：按个人或团队、领域和证据管理候选规则，显式审核后才能进入模型上下文。
- 模块—任务关系：用版本化关系记录任务正在建设、修改、使用、验证或阻塞的模块。
- 私人旧任务卡只读预览：生成通用 envelope、去重结果和状态差异，不直接写入公开运行库。

## 0.12.0

- 持久目标：保存目标范围、完成条件、续推预算、累计步数、Token 使用量和事件轨迹。
- 有界续推：依赖检查、人工裁决、路由、任务占用、结果验收和恢复门共用同一运行边界。
- 未知外部运行在恢复前停止，不盲目重放副作用。

## 0.11.0

- 工作进度按领域与工作线聚焦展示，切换页面只改变只读投影。
- 模块详情补充外部输入、内部处理、输出、接口协议和递归子图。
- `private-local` 与 `public-safe` 两种投影模式分别定义本地使用和公开展示边界。

## 0.10.0

- 系统地图支持递归下钻、真实连线、面包屑和内部模块结构。
- 工作流结构支持任务、顺序、分支、汇合、条件和有界循环。
- 展示包只改变界面文案与结构提示，不改变运行事实。

## 0.9.0

- 自动推进支持按任务能力、层级、上下文预算和可用性选择执行路线。
- 路由选择与真实 run 绑定，多个进程竞争时保留唯一有效执行证据。

## 0.8.0

- 增加有界自动推进：只处理依赖已满足、无待决策且处于队列中的任务。
- 模型结果停留在待验收状态；人工决定、阻塞和恢复门不会被自动越过。
- 增加引用式领域上下文编译器。

## 0.7.0

- 建立 Python、SQLite 与有限本地 API 运行底座。
- 保存工作流、任务、运行、事件、产物和决定，支持重启后读取历史状态。
- 运行上下文与本机路径留在服务端边界内。

## 0.6.0

- 发布匿名合成 Workbench 演示。
- 展示科研、会议纪要和深度分析报告的任务分配、运行轮次、事件轨迹和人工验收。
- 引入模块清单、能力供需关系和可替换执行入口。

各版本的详细任务边界、验收条件与设计依据见 [`docs/`](docs/) 下对应的开发任务书和参考说明。
