[metadata]
name = personal-ai-os-core
version = 0.11.0
description = Human-in-the-loop operating layer for long-running AI work
long_description = file: README.md
long_description_content_type = text/markdown
author = Dengk3Li

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.10
install_requires =

[options.packages.find]
where = src

[options.entry_points]
console_scripts =
    personal-ai-os = personal_ai_os.cli:main
